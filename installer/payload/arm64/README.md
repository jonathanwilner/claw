# arm64 Payload Slot

This directory is reserved for arm64-specific bundle content.

Examples of future staged assets:

- native Windows helpers compiled for arm64
- arm64-specific installer payloads
- architecture-specific configuration overrides
Place arm64-specific offline payloads here.

Supported conventions:

- `wsl.msi`
- `DockerDesktopInstaller.exe`
- `images/*.tar`
- `ollama-models/ollama-models.tar.gz`
