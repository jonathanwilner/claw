# x64 负载目录

[English](README.md)

该目录保留给 x64 专用 bundle 内容使用。

未来可预置资源示例：

- 为 x64 编译的原生 Windows 辅助工具
- 仅适用于 x64 的安装器负载
- 按架构区分的配置覆盖

将 x64 专用离线负载放在这里。

支持的约定：

- `wsl.msi`
- `DockerDesktopInstaller.exe`
- `images/*.tar`
- `ollama-models/ollama-models.tar.gz`
